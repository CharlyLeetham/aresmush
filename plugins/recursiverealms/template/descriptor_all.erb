  <%= line_with_text t('RecursiveRealms.descriptor_title') %>

<% if @descriptor && @descriptor.any? %>
<% @descriptor.each do |f| %>
  <%= left("%xhTitle:%xn #{f['Descriptor']}", 80) %>
  <% if f['Flavor Text'].present? %>
    <%= "%xhDescription: %xn #{f['Flavor Text']}" %>
  <% else %>
    <%= "%xnDescription: %xnNo description available." %>
  <% end %>

<% end %>
<% else %>
No descriptors available.
<% end %>
  <%= footer %>